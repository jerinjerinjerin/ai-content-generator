/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn-icons-png.flaticon.com', 't4.ftcdn.net','image.shutterstock.com'], // Add any other domains you need here
    },
  };
  
  export default nextConfig;
  